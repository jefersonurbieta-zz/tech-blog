<!-- Loop through the navigation items -->
{{#foreach navigation}}
    <a class="gh-navbar-item {{#if current}} nav-current{{/if}}" href="{{url absolute="true"}}">{{label}}</a>
{{/foreach}}
<!-- End the loop -->